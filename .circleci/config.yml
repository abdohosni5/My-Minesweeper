version: 2
orbs:
  newman: postman/newman@0.0.2
jobs:
  build:
    executor: newman/postman-newman-docker
    steps:
      - newman/newman-run:
          collection: https://github.com/abdohosni5/My-Minesweeper/blob/master/postman-api-testing/Users.postman_collection.json
          
workflows:
  example-workflow:
    jobs:
      - build